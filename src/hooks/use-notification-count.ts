// TODO (Phase 3): replace stub with real API call via src/api/notifications.ts
// Shape will be: const { data } = await apiClient.get<{ count: number }>("/notifications/count");

export function useNotificationCount(): number {
  return 0;
}